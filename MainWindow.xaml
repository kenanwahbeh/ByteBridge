<ui:FluentWindow x:Class="ByteBridge.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="ByteBridge"
        Width="900"
        Height="700"
        MinWidth="720"
        MinHeight="520"
        WindowStartupLocation="CenterScreen"
        WindowBackdropType="Mica"
        Loaded="Window_Loaded"
        Closing="Window_Closing"
        Closed="Window_Closed">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Menu bar -->
        <Menu x:Name="MainMenu"
              Grid.Row="0">

            <MenuItem x:Name="FileMenuItem">
                <MenuItem x:Name="NewDatabaseMenuItem" Click="NewDatabaseMenuItem_Click"/>
                <Separator/>
                <MenuItem x:Name="ExitMenuItem" Click="ExitMenuItem_Click"/>
            </MenuItem>

            <MenuItem x:Name="EditMenuItem">
                <MenuItem x:Name="EditSelectedMenuItem" IsEnabled="False" Click="EditSelectedMenuItem_Click"/>
                <MenuItem x:Name="DeleteSelectedMenuItem" IsEnabled="False" Click="DeleteSelectedMenuItem_Click"/>
            </MenuItem>

            <MenuItem x:Name="WebServerMenuItem" Click="WebServerMenuItem_Click"/>

            <MenuItem x:Name="CloudflareTunnelMenuItem" Click="CloudflareTunnelMenuItem_Click"/>

            <MenuItem x:Name="OptionsMenuItem" Click="OptionsMenuItem_Click"/>

            <MenuItem x:Name="HelpMenuItem">
                <MenuItem x:Name="AboutMenuItem" Click="AboutMenuItem_Click"/>
            </MenuItem>

        </Menu>

        <!-- Slim status readout -->
        <Border Grid.Row="1"
                Padding="20,10"
                BorderThickness="0,0,0,1"
                BorderBrush="#DCDCDC">

            <TextBlock x:Name="StatusTextBlock"
                       FontSize="13"/>

        </Border>

        <!-- Connections -->
        <ScrollViewer Grid.Row="2"
                      Margin="20"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">

            <StackPanel x:Name="ConnectionsPanel"/>

        </ScrollViewer>

    </Grid>

</ui:FluentWindow>
